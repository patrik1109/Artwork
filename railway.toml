[build]
builder = "nixpacks"

[deploy]
startCommand = "java -jar target/artgallery-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10 